var store = ["a - apple", "b - bird", "c - cat", "default - stuff "];

const switchOfStuff = (val) => {
  let answer = "";
  // Only change code below this line
  console.log(store[0]);
  // Only change code above this line
  return answer;
};

switchOfStuff(1);
